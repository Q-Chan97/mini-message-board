<% if (locals.errors) { %>
    <ul>
        <% errors.forEach((error) => { %>
            <li style="color: red; list-style-type: katakana-iroha;">
                <%= error.msg %>
            </li>
        <% }); %>
    </ul>
<% } %>